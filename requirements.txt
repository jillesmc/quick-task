# Requirements for Jira Quick Task
# 
# NOTA IMPORTANTE: PySide6 deve ser instalado via pacotes do sistema para 
# compatibilidade com Kirigami 6 (KF6) e plugins QML do sistema:
#   sudo apt install python3-pyside6.qtcore python3-pyside6.qtgui python3-pyside6.qtqml python3-pyside6.qtwidgets
#
# Instale as dependências com:
#   pip3 install -r requirements.txt

# Dependência para atalho global (Super+J)
# python-xlib deve ser instalado via pacote do sistema:
#   sudo apt install python3-xlib
# Não incluímos aqui porque é um pacote do sistema, não um pacote pip

# Dependências para integração com Jira (REST API para worklogs)
requests>=2.31.0
pytz>=2023.3
pyyaml>=6.0

# Entrada por voz (gravação de áudio; transcrição/LLM via Ollama no host)
sounddevice>=0.4.6
scipy>=1.11.0
numpy>=1.24.0

# Apenas para desenvolvimento/testes:
pytest>=7.0.0
pytest-mock>=3.10.0
pytest-qt>=4.2.0
pytest-cov>=4.1.0
black>=23.0.0
