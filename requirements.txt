# Requirements for Jira Quick Task
# 
# NOTA IMPORTANTE: PySide2 deve ser instalado via pacotes do sistema para 
# compatibilidade com Kirigami 2 (KF5) e plugins QML do sistema:
#   sudo apt install python3-pyside2.qtcore python3-pyside2.qtgui python3-pyside2.qtqml
#
# O projeto usa mise para gerenciar a versão do Python e pacotes do sistema
# para PySide2. Instale as dependências com:
#   mise run -- pip install -r requirements.txt

# Dependência para atalho global (Super+J)
# python-xlib deve ser instalado via pacote do sistema:
#   sudo apt install python3-xlib
# Não incluímos aqui porque é um pacote do sistema, não um pacote pip

# Dependências para integração com Jira (REST API para worklogs)
requests>=2.31.0
pytz>=2023.3
pyyaml>=6.0

# Apenas para desenvolvimento/testes:
pytest>=7.0.0
pytest-mock>=3.10.0
pytest-qt>=4.2.0
pytest-cov>=4.1.0
black>=23.0.0
